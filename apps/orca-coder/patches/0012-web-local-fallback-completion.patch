diff --git a/src/renderer/src/lib/terminal-worktree-route.ts b/src/renderer/src/lib/terminal-worktree-route.ts
index fc001125d..a0bd700b4 100644
--- a/src/renderer/src/lib/terminal-worktree-route.ts
+++ b/src/renderer/src/lib/terminal-worktree-route.ts
@@ -5,6 +5,7 @@ import type { AppState } from '@/store/types'
 import { getRuntimeEnvironmentIdForWorktree } from './worktree-runtime-owner'
 import { resolveWorktreeOperationRouteResult } from './worktree-operation-route'
 import { getSingleFocusedRuntimeEnvironmentId } from './single-runtime-legacy-owner'
+import { getWebClientLocalFallbackEnvironmentId } from './floating-workspace-runtime-owner'
 
 export type TerminalWorktreeRoute = {
   runtimeEnvironmentId: string | null
@@ -28,7 +29,15 @@ export function resolveTerminalWorktreeRoute(
   // the active runtime — so a remote skill install lands on that runtime — falling back to local
   // when none is focused, instead of failing them closed.
   if (isEphemeralSetupTerminalWorktreeId(worktreeId)) {
-    return { runtimeEnvironmentId: getSingleFocusedRuntimeEnvironmentId(state) }
+    // Why the extra fallback: getSingleFocusedRuntimeEnvironmentId refuses to answer
+    // unless exactly one environment is saved AND it is the focused one — a sensible
+    // guard for a desktop app, but its "fall back to local" outcome is unreachable in a
+    // web client, where local means a rejecting pty.spawn stub. The orchestration skill
+    // installer runs in one of these terminals.
+    return {
+      runtimeEnvironmentId:
+        getSingleFocusedRuntimeEnvironmentId(state) ?? getWebClientLocalFallbackEnvironmentId(state)
+    }
   }
   const resolution = resolveWorktreeOperationRouteResult(state, worktreeId)
   if (resolution.kind === 'resolved') {
diff --git a/src/renderer/src/lib/worktree-runtime-owner.ts b/src/renderer/src/lib/worktree-runtime-owner.ts
index 9a216f358..3f6b42c17 100644
--- a/src/renderer/src/lib/worktree-runtime-owner.ts
+++ b/src/renderer/src/lib/worktree-runtime-owner.ts
@@ -16,7 +16,10 @@ import {
   resolveIndexedWorktreeOwner
 } from './worktree-runtime-owner-index'
 import { getSingleFocusedRuntimeEnvironmentId } from './single-runtime-legacy-owner'
-import { getFloatingWorkspaceRuntimeEnvironmentId } from './floating-workspace-runtime-owner'
+import {
+  getFloatingWorkspaceRuntimeEnvironmentId,
+  getWebClientLocalFallbackEnvironmentId
+} from './floating-workspace-runtime-owner'
 import {
   getExecutionHostIdForFolderWorkspace,
   getExplicitRuntimeEnvironmentIdForFolderWorkspace,
@@ -207,6 +210,12 @@ export function getSettingsForWorktreeRuntimeOwner(
 ): Pick<GlobalSettings, 'activeRuntimeEnvironmentId'> {
   return {
     ...state.settings,
-    activeRuntimeEnvironmentId: getRuntimeEnvironmentIdForWorktree(state, worktreeId)
+    // Why: this helper decides the runtime target for callers that spawn PTYs DIRECTLY —
+    // launch-agent-background-session and launch-worktree-background-terminals — which
+    // never reach the pty-connection transport and so never see its web-client floor.
+    // A local answer sends them at window.api.pty.spawn, a rejecting stub in the browser.
+    activeRuntimeEnvironmentId:
+      getRuntimeEnvironmentIdForWorktree(state, worktreeId) ??
+      getWebClientLocalFallbackEnvironmentId(state)
   }
 }
diff --git a/src/renderer/src/web/web-preload-api.ts b/src/renderer/src/web/web-preload-api.ts
index dbe4ae9ce..2b91eb09b 100644
--- a/src/renderer/src/web/web-preload-api.ts
+++ b/src/renderer/src/web/web-preload-api.ts
@@ -7,6 +7,7 @@ import type {
   NativeChatAppendedMessages
 } from '../../../preload/api-types'
 import type { RuntimeRpcResponse } from '../../../shared/runtime-rpc-envelope'
+import { floatingWorkspaceToWorktree } from '../../../shared/floating-workspace-worktree'
 import type { AiVaultListArgs, AiVaultListResult } from '../../../shared/ai-vault-types'
 import type {
   AiVaultPrepareSessionResumeArgs,
@@ -4039,11 +4040,38 @@ async function resolveRuntimeWorktreeByPath(
     .filter((entry) => entry.relativePath !== null)
     .sort((a, b) => b.worktree.path.length - a.worktree.path.length)[0]
   if (!match) {
+    // Why: the floating workspace is a real directory on the host but never a detected
+    // worktree, so path->worktree resolution rejected everything under it — including
+    // `<floating>/.orca/templates`, which the markdown-note flow probes before creating
+    // a file. Same synthetic record the runtime resolves the sentinel to.
+    const floatingPath = await getFloatingWorkspacePathForResolution(callResult)
+    if (floatingPath && relativePathInsideRoot(floatingPath, worktreePath) !== null) {
+      return floatingWorkspaceToWorktree(floatingPath)
+    }
     throw new Error(`No runtime worktree owns ${worktreePath}`)
   }
   return match.worktree
 }
 
+let floatingWorkspacePathForResolution: string | null = null
+
+// Why: one RPC per session — the host's floating workspace directory is fixed for the
+// lifetime of a serve process, and this sits on a path-resolution hot path.
+async function getFloatingWorkspacePathForResolution(
+  callResult: WebRuntimeResultCaller
+): Promise<string | null> {
+  if (floatingWorkspacePathForResolution !== null) {
+    return floatingWorkspacePathForResolution
+  }
+  try {
+    const result = await callResult<{ path: string }>('floatingWorkspace.markdownDirectory', {})
+    floatingWorkspacePathForResolution = result.path?.trim() || null
+  } catch {
+    floatingWorkspacePathForResolution = null
+  }
+  return floatingWorkspacePathForResolution
+}
+
 async function resolveRuntimeFilePath(
   filePath: string,
   preferredWorktreePath?: string,
