# Gitignored reference clones — study upstreams here, never a build input.
.upstream/

# AppImage build export dir (buildx --output type=local)
dist/

# Python bytecode from the agent-tooling scripts under .claude/skills/
__pycache__/
